<div class="director">
	<%= image_tag director[:img_url], alt: director[:name], :class => 'director-img' %>
	<p class="lead"><%= director[:name] %></p>
	<strong><mark><%= films.length %> Films</mark></strong>
</div>
